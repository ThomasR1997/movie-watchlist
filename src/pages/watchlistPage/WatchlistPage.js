import { StyledMessage } from "../../components/StyledComponents";

export const WatchlistPage = () => {
  return (
    <div>
      <StyledMessage>Your watchlist is looking a little empty...</StyledMessage>
    </div>
  );
};
